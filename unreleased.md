# Unreleased

## Features

- Route parameters are now supported and can be used when;
  - navigating through pages
  - fetching data from backend
  - performing mutation actions


## Improvements

- `Parameters` now accept parameter class attribute for each parameter
