locatable-label @controller-model {
  with {
    @action-model route="GET /locatable-labels/{id}" required-claims="BaseA, BaseB"
    id {
      @parameter-model required=#true in="route"
    }
  }
}

